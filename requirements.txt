# Serveur MCP - Dépendances de Production
# Version Python: 3.10+

# Core dependencies
pydantic>=2.0,<3.0

# Phase 3: Authentication
PyJWT>=2.8,<3.0          # JWT generation/validation
bcrypt>=4.1,<5.0        # Password hashing

# Phase 4: Network Transports
aiohttp>=3.8,<4.0        # HTTP server and WebSocket
# websockets>=11.0,<12.0 (aiohttp includes websockets)
